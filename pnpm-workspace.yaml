packages:
  - 'apps/*'
  - 'packages/*'

# esbuild ships a platform binary via a postinstall script. Vite and tsx both
# need it, so the script is allowed explicitly rather than globally.
allowBuilds:
  esbuild: true
