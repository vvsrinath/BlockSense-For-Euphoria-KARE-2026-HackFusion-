{
  "name": "@blocksense/api",
  "version": "0.1.0",
  "private": true,
  "description": "The BlockSense HTTP API. Serves the web app and any external client.",
  "author": "Srinath Vatchavari Venkateshan <vvsrinath0@gmail.com>",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc --noEmit -p tsconfig.json && node scripts/build.mjs",
    "start": "node dist/index.js",
    "start:dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@blocksense/blockchain": "workspace:*",
    "@blocksense/intelligence": "workspace:*",
    "@blocksense/shared": "workspace:*",
    "@blocksense/transaction-engine": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "tsx": "^4.19.0"
  }
}
