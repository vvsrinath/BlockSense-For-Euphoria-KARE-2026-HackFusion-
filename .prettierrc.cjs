// The single source of truth lives in config/prettier so the formatting rules
// are reviewable in one place alongside the rest of the tooling config.
module.exports = require('./config/prettier/index.cjs');
